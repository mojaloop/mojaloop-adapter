apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lpsadaptor.fullname" . }}-test-connection"
  labels:
{{ include "lpsadaptor.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "lpsadaptor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
