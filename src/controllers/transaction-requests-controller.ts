import { Request, ResponseToolkit } from 'hapi'
import { TransactionRequest } from 'services/transaction-request-service'

// TODO: create endpoints for POST and PUT

// export async function create (request: Request, toolkit: ResponseToolkit): Promise<TransactionRequest> {
//   const transactionRequestService = request.app.transactionRequestService
// }
